<!DOCTYPE html>
<html lang="fr">
<head>
    <meta name="viewport" content="width=device-width, maximum-scale=1" />
    <link rel="icon" href="/assets/images/favicon-32x32.ico"/>
    <meta charset="UTF-8" />
    <title>AMOS FAMES - Accueil</title>
    <style>
        /* Général */
        body, html {
            margin: 0;
            padding: 0;
            font-family: "Helvetica Nue", sans-serif;
        }

        /* Section avec l'image de fond */
        .image-section {
            position: relative;
            width: 100%;
            height: 100vh;
            overflow: hidden;
        }

        .background-image {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            object-fit: cover;
            filter: brightness(50%);
        }

        /* Contenu centré */
        .head-content {
            position: relative;
            z-index: 2;
            display: flex;
            flex-direction: column;
            align-items: flex-start; /* aligne le texte à gauche */
            justify-content: center;
            height: 100%;
            padding-left: 5%;
            color: white;
        }

        /* Titre principal */
        .headline-text {
            font-size: 5rem;
            line-height: 1.2;
        }

        .headline-text_start {
            display: block;
            color: #008b57; /* vert */
        }

        .headline-text_end {
            display: block;
            color: white; /* blanc */
        }

        /* Bouton */
        .button1 {
            display: inline-block;
            margin-top: 20px;
            padding: 12px 24px;
            background-color: #008b57;
            color: white;
            text-decoration: none;
            font-size: 1rem;
            border-radius: 10px;
            transition: background 0.3s ease, transform 0.2s ease;
        }

        .button1:hover {
            background-color: #00a34a;
        }

        .forum-section {
            display: flex;
            align-items: center;
            max-width: 1200px;
            margin: 150px auto;
            background-color: transparent;
            border-radius: 0;
            overflow: hidden;
            height: 400px; /* Réduction de la hauteur */
        }

        .forum-text {
            flex: 0 0 500px;
            padding: 30px;
            background-color: #008b57;
            color: white;
            height: 100%;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .text-title {
            font-size: 48px;
            font-weight: bold;
            font-family: Arial, sans-serif;
            margin-bottom: 20px;
            color: white;
            text-transform: uppercase;
            letter-spacing: 1px;
            line-height: 1;
        }

        .text-main {
            font-size: 16px;
            line-height: 1.6;
            font-family: Arial, sans-serif;
        }

        .forum-section img {
            flex: 1;
            height: 100%;
            object-fit: cover;
        }

    </style>
    <script src="/assets/script/script.js"></script>
</head>
<body>
{% include "navbar.html.twig" %}

<main class="homepage">
    <section class="image-section">
        <img class="background-image" src="/assets/images/homeImage.png" alt="Image d'accueil">
        <div class="head-content">
            <div class="headline-text">
                <span class="headline-text_start">Inscrivez vous</span>
                <span class="headline-text_end">au FAMES</span>
            </div>
            <div class="head-action">
                <a href="/forum" class="button1" onclick="handleForumClick()">Inscrivez-vous</a>
            </div>
        </div>
    </section>
    <section class="forum-section">
        <div class="forum-text">
            <h2 class="text-title">Le Forum</h2>
            <p class="text-main">Le forum est vraiment top Lorem Ipsum et tout</p>
        </div>
        <img src="/assets/images/fames_illustration.jpg" alt="photo du FAMES">
    </section>
</main>
{% include "footer.html.twig" %}
</body>
</html>
